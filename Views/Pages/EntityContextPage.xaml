<Page
  x:Class="UIDesignerWPFUI.Views.Pages.EntityContextPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:UIDesignerWPFUI.Views.Pages"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
  Title="DashboardPage"
  d:DataContext="{d:DesignInstance local:EntityContextPage,
                                     IsDesignTimeCreatable=False}"
  d:DesignHeight="450"
  d:DesignWidth="800"
  ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
  ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
  Foreground="{DynamicResource TextFillColorPrimaryBrush}"
  mc:Ignorable="d"
>
  <Grid VerticalAlignment="Top">
  
      <ui:NavigationView Grid.Row="0"
                         x:Name="RootNavigation"
                         Padding="42,0,42,0"
                         BreadcrumbBar="{Binding ElementName=BreadcrumbBar}"
                         FrameMargin="0"
                         IsBackButtonVisible="Visible"
                         IsPaneToggleVisible="True"
                   
                         OpenPaneLength="310"
                         PaneDisplayMode="Top"
                         TitleBar="{Binding ElementName=TitleBar, Mode=OneWay}"
      >
          <ui:NavigationView.Header>
              <ui:BreadcrumbBar x:Name="BreadcrumbBar" Margin="42,32,42,20" />
          </ui:NavigationView.Header>
          <ui:NavigationView.ContentOverlay>
              <Grid>
                  <ui:SnackbarPresenter x:Name="SnackbarPresenter" />
              </Grid>
          </ui:NavigationView.ContentOverlay>

          <ui:NavigationView.MenuItems>
              <ui:NavigationViewItem
                  Content="Entity context"
                  Icon="Home24"
                  TargetPageType="{x:Type local:EntityContextData}" />

              <ui:NavigationViewItem
                  Content="Grid"
                  Icon="Table24"
                  TargetPageType="{x:Type local:GridDataPage}" />

              <ui:NavigationViewItem
                  Content="Item"
                  Icon="Table24"
                  TargetPageType="{x:Type local:ItemDataPage}" />


          </ui:NavigationView.MenuItems>
      </ui:NavigationView>
    </Grid>
</Page>
