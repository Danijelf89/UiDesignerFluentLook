<Page
  x:Class="UIDesignerWPFUI.Views.Pages.LookupPage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:UIDesignerWPFUI.Views.Pages"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:models="clr-namespace:UIDesignerWPFUI.Models"
  xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
  Title="DataPage"
  d:DataContext="{d:DesignInstance local:LookupPage,
                                     IsDesignTimeCreatable=False}"
  
  ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
  ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
  Foreground="{DynamicResource TextFillColorPrimaryBrush}"
  ScrollViewer.CanContentScroll="False"
  mc:Ignorable="d"
>
  <Grid VerticalAlignment="Stretch">
      
          <Grid.ColumnDefinitions>
              <ColumnDefinition Width="200" MinWidth="100"></ColumnDefinition>
              <ColumnDefinition Width="5"></ColumnDefinition>
              <ColumnDefinition  MinWidth="200"></ColumnDefinition>
            </Grid.ColumnDefinitions>

          
          <ui:ListView Grid.Column="0" VerticalAlignment="Stretch">
              <ui:ListViewItem Content="Lookup 1"></ui:ListViewItem>
              <ui:ListViewItem Content="Lookup 2"></ui:ListViewItem>
              <ui:ListViewItem Content="Lookup 3"></ui:ListViewItem>
              <ui:ListViewItem Content="Lookup 4"></ui:ListViewItem>
            </ui:ListView>

          <GridSplitter Grid.Column="1" ResizeBehavior="PreviousAndCurrent" Width="2" Margin="2" ResizeDirection="Columns"></GridSplitter>

      <GroupBox Grid.Column="2" Header="Lookup Data" VerticalAlignment="Top" Height="Auto" Margin="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition ></RowDefinition>
                    <RowDefinition ></RowDefinition>
                    <RowDefinition></RowDefinition>
                    <RowDefinition></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"></ColumnDefinition>
                    <ColumnDefinition Width="5"></ColumnDefinition>
                    <ColumnDefinition Width="*"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <ui:TextBlock HorizontalAlignment="Right" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="Lookup name:" TextWrapping="Wrap"></ui:TextBlock>
                <ui:TextBox Grid.Row="0" Grid.Column="2" Text="Lookup 1"></ui:TextBox>

                <ui:TextBlock Grid.Row="1" Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Center" Text="Destination fields:" TextWrapping="Wrap"></ui:TextBlock>
                <ui:TextBox Grid.Row="1" Grid.Column="2" Text="Id; Name"></ui:TextBox>


                <CheckBox Grid.Row="2" Grid.Column="2" IsChecked="True"/>

                <ui:Button Grid.Row="3" Grid.Column="2" Click="ButtonBase_OnClick" Content="Localize"/>
            </Grid>


      </GroupBox>
          
        
      

<!--<ui:VirtualizingItemsControl
      Foreground="{DynamicResource TextFillColorSecondaryBrush}"
      ItemsSource="{Binding ViewModel.Colors, Mode=OneWay}"
      VirtualizingPanel.CacheLengthUnit="Item"
    >
      <ItemsControl.ItemTemplate>
        <DataTemplate DataType="{x:Type models:DataColor}">
          <ui:Button
            Width="80"
            Height="80"
            Margin="2"
            Padding="0"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Appearance="Secondary"
            Background="{Binding Color, Mode=OneWay}"
            FontSize="25"
            Icon="Fluent24"
          />
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ui:VirtualizingItemsControl>-->
  </Grid>
</Page>
